#include<stdio.h>

int main(int h, int m)
{
  h = 21;
  while (h < 24)
    {
      m = 41;
      while (m < 60 )
	{
	  putchar((h / 10) + '0');
	  putchar((h % 10) + '0');
	  putchar(':');
	  putchar((m / 10) + '0');
	  putchar((m % 10) + '0');
	  putchar('\n');
	  m++;
	}
      h++;
	}
}
